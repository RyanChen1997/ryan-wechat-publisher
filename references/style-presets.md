# 内置排版风格预设

## 深色模式（夜间模式）适配规范

微信夜间模式不是“换肤”，而是对文章里**每一个显式设置的颜色**跑一遍反色算法（`wechatjs/mp-darkmode`）：

| 颜色情况 | 夜间结果 | 说明 |
|---------|---------|------|
| 白 / 近白背景（灰阶 L>40% 或感知亮度>250） | 亮度取反到 `#191919` 附近 | 与页面底色接近，本身不算突兀 |
| 浅色淡彩背景（感知亮度 190~250，如 `#f0f6fc`、`rgb(231,244,255)`） | **压成中灰色块** | 这就是“深浅不一马赛克”的来源 |
| 半透明白背景（`rgba(255,255,255,0.x)`） | 说不清的中灰 | 禁用 |
| 高饱和中间调（品牌蓝/橙等） | 保持不变 | 可放心用作边框、色块、强调文字 |
| 深色文字 | 反白/提亮 | 保留深色即可，夜间自动可读 |
| `var(--weui-*)` | 不被算法处理，由客户端解析成原生深色 | 最可靠的“自适应”手段 |

### 预设配色三原则（所有预设已遵守，新增预设必须遵守）

1. **页面底色**：`background-color: var(--weui-BG-1, <白天色>)` — 白天≈原色（`#F7F7F7`），夜间 `#1E1E1E` 原生页面色
2. **卡片 / 引用 / 列表 / 章节框等浅色块**：`background-color: var(--weui-BG-2, <白天色>)` — 白天 `#FFFFFF`，夜间 `#191919` 原生卡片色
3. **代码（行内与代码块）**：`background-color: var(--weui-BG-3, <白天色>)` — 白天 `#F7F7F7`，夜间 `#404040` 原生代码色，夜间仍可见

fallback 写当前白天色：本地预览、非微信环境显示的仍是原来的颜色，白天效果不变；夜间由微信客户端解析变量为原生深色，整篇只有 2~3 个统一灰阶，不再有马赛克。

> 本地验证：在排版预览器（`04-html/studio.html`）里切右上角「夜间」直接看 —— 预览器的夜间模式就是按下面这个算法渲染的，不需要另开页面。
> 只有在单独调预设、手上没有预览器时，才用 `node scripts/utils/dark-preview.js <预览.html> [输出.html]` 离线生成一张。

---

## 分类总览

模板按分类归档在 `scripts/presets/<分类 id>/<preset-id>/`。当前 7 套模板分布在 6 个分类里，「动漫」「文艺」「复古」「中国风」还没有模板（目录与分类桶已建好，画廊会自动隐藏它们，以后加了模板就自动出现）。

| 分类 | 目录 | 套数 | 这一类的模板 |
|------|------|------|--------------|
| 商务 | `scripts/presets/business/` | 1 | 公众号蓝黄线框 |
| 简约 | `scripts/presets/simple/` | 2 | 蓝点笔记、极客科技 |
| 清新 | `scripts/presets/fresh/` | 1 | 蓝紫徽章 |
| 卡通 | `scripts/presets/cartoon/` | 1 | 童趣手绘 |
| 时尚 | `scripts/presets/fashion/` | 1 | 活力徽章 |
| 极简 | `scripts/presets/minimal/` | 1 | 极简雅致 |
| 动漫 | `scripts/presets/anime/` | 0 | （暂无模板，目录与分类桶已保留） |
| 文艺 | `scripts/presets/literary/` | 0 | （暂无模板，目录与分类桶已保留） |
| 复古 | `scripts/presets/retro/` | 0 | （暂无模板，目录与分类桶已保留） |
| 中国风 | `scripts/presets/chinese/` | 0 | （暂无模板，目录与分类桶已保留） |

分类的顺序、中文名与说明写在根注册器 `scripts/presets/index.js`（`CATEGORY_ORDER`）+ 各分类目录下的 `index.js`（分类桶）；画廊按这个顺序排列分类标签页，并直接复用分类说明作为标签的悬停提示。

## 预设列表

下面 7 套是本 skill 的全部内置模板。括号里是 `preset-id`，圆括号后面是所属分类。

### 1. 蓝点笔记（blue-dot-notes）

| 属性 | 值 |
|------|-----|
| **风格** | 清爽蓝白配色，蓝色圆点装饰一级标题，简约克制 |
| **适合场景** | 职场干货、方法论、科技科普、工具测评、效率提升 |
| **主色** | `rgb(2, 183, 237)` |
| **背景色** | `#ffffff` |
| **正文色** | `rgb(62, 62, 62)` |
| **正文字号** | 15px |
| **行高** | 1.93 |
| **一级标题** | 22px 加粗 + 22px 蓝色圆点（CSS 绘制，带白边内发光） |
| **二级标题** | 18px 加粗 |
| **三级标题** | 16px 加粗 |
| **图片** | 居中，全宽 |
| **引用块** | 蓝色左边框 + 浅蓝背景 |
| **列表** | 蓝色圆点项目符号 |
| **分割线** | 1px 浅灰细线 |
| **段落间距** | 空行法（秀米类模板风格） |

**模板来源**：某职场科技类公众号

**夜间模式**：✅ 已适配 — 页面透明（夜间原生黑底），引用 `--weui-BG-2`，代码 `--weui-BG-3`，青色点缀保留

---

### 2. 蓝紫徽章（purple-badge）

| 属性 | 值 |
|------|-----|
| **风格** | 蓝紫主色，数字徽章标题装饰，现代清新有呼吸感 |
| **适合场景** | 成长感悟、心理情感、生活方式、个人提升、女性向内容 |
| **主色** | `rgb(105, 130, 250)` |
| **背景色** | `#ffffff` |
| **正文色** | `rgb(47, 47, 47)` |
| **正文字号** | 16px |
| **行高** | 1.6em |
| **字间距** | 2px |
| **一级标题** | 26px 蓝紫色加粗 + 数字徽章 SVG 装饰（渐变色+星星点缀） |
| **二级标题** | 18px 深灰加粗，字间距 2px |
| **三级标题** | 17px 深灰加粗，字间距 1px |
| **图片** | 圆角 12px，居中 |
| **引用块** | 蓝色左边框 + 浅蓝背景 |
| **列表** | 蓝色小圆点 |
| **分割线** | 40% 宽浅灰细线（居中） |
| **段落间距** | margin-bottom: 24px |

**模板来源**：壹心理风格

**夜间模式**：✅ 已适配 — 页面透明（夜间原生黑底），引用 `--weui-BG-2`，代码 `--weui-BG-3`，蓝紫强调色保留

---

### 3. 极简雅致（elegant-minimal）

| 属性 | 值 |
|------|-----|
| **风格** | 灰白极简，双圆点 ○● 标题装饰，大行距两端对齐，雅致克制 |
| **适合场景** | 设计美学、成长感悟、生活方式、知识分享、职场思考 |
| **主色** | `rgba(0, 0, 0, 0.9)` |
| **点缀色** | `rgb(61, 170, 214)` |
| **背景色** | `#ffffff` |
| **正文色** | `rgb(102, 102, 102)` |
| **正文字号** | 15px |
| **行高** | 2em |
| **字间距** | 0.544px |
| **一级标题** | 20px 居中加粗 + ○● 双圆点符号装饰 |
| **二级标题** | 17px 左对齐加粗 |
| **三级标题** | 15px 左对齐加粗 |
| **图片** | 居中，全宽 |
| **引用块** | 青蓝左边框 + 浅蓝背景 |
| **列表** | 深灰圆点项目符号 |
| **分割线** | 短灰线（居中） |
| **段落间距** | 空行法 |

**模板来源**：某设计类公众号（复刻版）

**夜间模式**：✅ 已适配 — 页面 `--weui-BG-2`，引用 `--weui-BG-2`，代码 `--weui-BG-3`，文字夜间由算法反白

---

### 4. 活力徽章（vibrant-badge）

| 属性 | 值 |
|------|-----|
| **风格** | 蓝橙撞色，PART 倾斜徽章 + 蓝色横条双层标题，白色圆角卡片 + 左侧螺旋装订装饰，活泼有设计感 |
| **适合场景** | 职场干货、效率工具、方法论、成长感悟、产品测评 |
| **主色** | `#1266be` |
| **点缀色** | `#e36c09` |
| **背景色** | `#f5f5f5`（浅灰） |
| **正文色** | `#333333` |
| **正文字号** | 15px |
| **行高** | 2 |
| **字间距** | 1.5px |
| **一级标题** | 橙色 PART 倾斜徽章（上方）+ 蓝色横条标题（下方带放大镜图标） |
| **二级标题** | 15px 蓝色加粗 |
| **三级标题** | 15px 橙色加粗 |
| **内容卡片** | 白色圆角卡片 + 左侧蓝色螺旋装订装饰 |
| **图片** | 圆角 8px，居中 |
| **引用块** | 蓝色左边框 + 浅蓝背景，圆角右侧 |
| **列表** | 蓝色圆点项目符号 |
| **分割线** | 渐变线（居中） |
| **段落间距** | margin-bottom: 14px |

**模板来源**：135编辑器 AI 排版推广文（复刻版）

**夜间模式**：✅ 已适配 — 页面底色 `--weui-BG-1`，卡片/引用 `--weui-BG-2`，代码 `--weui-BG-3`，蓝橙徽章/标题块保留

---

### 5. 极客科技（geek-tech）

| 属性 | 值 |
|------|-----|
| **风格** | 蓝色数字编号 + 黄色装饰条 + 居中标题，简约专业的科技媒体风 |
| **适合场景** | 科技资讯、职场干货、行业分析、产品解读、AI 工具 |
| **主色** | `rgb(45, 113, 214)` |
| **点缀色** | `#fee2b9`（浅黄） |
| **背景色** | `#ffffff` |
| **正文色** | `rgb(0, 0, 0)` |
| **正文字号** | 16px |
| **行高** | 1.6 |
| **一级标题** | 居中蓝色数字编号（24px 斜体）+ 下方黄色装饰条 + 黑色标题文字 |
| **二级标题** | 17px 深灰加粗，居中，字间距 1px |
| **三级标题** | 16px 深蓝灰加粗 |
| **图片** | 全宽，无圆角 |
| **引用块** | 蓝色左边框 + 浅蓝背景 |
| **列表** | 蓝色圆点项目符号 |
| **分割线** | 40% 宽浅灰细线（居中） |
| **段落间距** | margin-bottom: 20px |

**模板来源**：极客公园「极客一问」（复刻版）

**夜间模式**：✅ 已适配 — 页面透明（夜间原生黑底），引用 `--weui-BG-2`，代码 `--weui-BG-3`，蓝色强调色保留

---

### 6. 公众号蓝黄线框（wechat-blue-yellow）

| 属性 | 值 |
|------|-----|
| **风格** | 浅蓝灰背景，引言虚线框，每个一级章节独立成框（白实线框/半透明白实线框/浅蓝圆角块），深蓝标题块配黄色右边条（居中），蓝黄撞色 |
| **适合场景** | AI 工具、科技科普、职场干货、方法论、新闻资讯 |
| **主色** | `rgb(0, 95, 199)` |
| **点缀色** | `rgb(251, 197, 24)`（黄） |
| **背景色** | `rgb(247, 249, 252)`（浅蓝灰） |
| **正文色** | `rgb(62, 62, 62)` |
| **正文字号** | 15px |
| **行高** | 2 |
| **字间距** | 0.3px |
| **章节标题** | 深蓝色块 + 白字 + 右侧 5px 黄色边条，居中，位于章节框外上方 |
| **章节容器** | 每个 `##` 章节独立成框：第 1-2 章白色实线框、第 3-4 章半透明白实线框、结尾浅蓝圆角块；引言区用蓝色虚线框 |
| **三级标题** | 浅蓝底 + 黄色左边框，深蓝字 |
| **正文段落** | 首行缩进 2em，两端对齐（模板特征） |
| **图片** | 居中，全宽，无圆角 |
| **引用块** | 蓝色左边框 + 浅蓝背景 |
| **列表** | 黄色圆点项目符号，浅蓝底 |
| **代码块** | 浅蓝底 + 淡蓝边框 |
| **链接** | 不用 `<a>` 标签（微信会过滤），改为「标题（URL）」纯文本展示 |
| **段落间距** | margin-bottom: 10px |

**模板来源**：某机构新闻类公众号（复刻版）

**夜间模式**：✅ 已适配 — 页面底色 `--weui-BG-1`，各章节框/引言/引用/列表 `--weui-BG-2`，代码 `--weui-BG-3`；深蓝标题块与蓝黄边框保留，夜间为统一深底 + 彩色线框

> [!note] 依赖引擎能力
> 本预设使用「章节容器」模式（`openSection`/`closeSection` + `sectionHeadingLevel`），需要渲染引擎支持（`scripts/presets/base.js`）。该能力向后兼容，不影响其他预设。

---

### 7. 童趣手绘（childlike-doodle）

| 属性 | 值 |
|------|-----|
| **风格** | 原文章手绘风格，顶部插画 + 手绘数字 + 黄色干刷标题三段式组合 |
| **适合场景** | AI 工具、创作方法、轻松科普、自动化演示 |
| **主色** | `#72BCEA`（天空蓝） |
| **点缀色** | `#FFE36F`（笔刷黄）、`#FF6576`（珊瑚红） |
| **正文色** | `#3E3E3E` |
| **正文字号** | 16px |
| **行高** | 2 |
| **一级标题** | 顶部 GIF 插画 + SVG 生成的手绘数字 PNG + 动态黄色干刷标题 PNG |
| **加粗高亮** | 田园橙、天空蓝、嫩叶绿、珊瑚红按出现顺序循环 |
| **数字素材** | 预渲染 1～10，超过 10 时按同一 SVG 模板动态生成 |
| **标题缓存** | 默认写入系统临时目录，不污染 Git 仓库 |

**模板来源**：公众号文章手绘图片标题复刻

**夜间模式**：✅ 已适配 — 页面背景使用 `--weui-BG-2`；标题装饰已转 PNG/GIF，蓝黄红强调色保留

---

## 如何选择

| 你的文章类型 | 推荐预设 | 原因 |
|-------------|---------|------|
| 职场/科技/工具评测 | 蓝点笔记 | 蓝白配色专业理性，适合干货输出 |
| 成长/情感/生活方式 | 蓝紫徽章 | 蓝紫温柔清新，大间距更有呼吸感 |
| 设计/美学/有气质的内容 | 极简雅致 | 灰白极简，双圆点标题，留白充分显高级 |
| 想要活泼撞色 + 卡片式排版 | 活力徽章 | 蓝橙对比强，PART 徽章 + 卡片结构视觉冲击力强 |
| 科技资讯/行业分析/AI 内容 | 极客科技 | 数字编号标题有辨识度，科技媒体风格 |
| AI 工具/科普/方法论 | 公众号蓝黄线框 | 章节独立框结构清晰，蓝黄撞色有辨识度 |
| 轻松科普/创作方法/手绘风 | 童趣手绘 | 三段式图片标题辨识度高，支持动态标题和多色高亮 |
| 需要强视觉冲击力 | （复刻模式） | 找一篇喜欢的公众号文章直接复刻 |

---

## 新增预设

新增一个预设包只需：

1. 先想好它属于哪个分类，在 `scripts/presets/<分类 id>/` 下复制一个现有预设包（如 `simple/blue-dot-notes/`）；拿不准就打 `node -e "const {listCategories}=require('./scripts/presets/index');for(const c of listCategories())console.log(c.id, c.name, c.description)"` 看分类说明
2. 在新包的 `index.js` 中修改 `id`、`name`、`tagline`、`description`、`suitableFor`、`meta`
3. 调整 `STYLES` 对象中的样式值
4. 如果有特殊装饰，在 `decorations` 中自定义对应 `makeXxx` 函数；专属 SVG、PNG、GIF 放到包内的 `svg/` 或 `assets/`
5. 在该分类的 `index.js`（分类桶）里加一行 `require('./<preset-id>/index')`，**按 preset-id 字母序插到对应位置**（不用改根 `index.js`）
6. **夜间模式适配**（必做）：浅色块一律写 `var(--weui-BG-1/2/3, <白天色>)`，不写半透明白背景；`meta.darkMode` 标注适配方式；改完在预览器里切「夜间」检查效果
7. 跑 `npm test` 回归：注册器会校验分类桶 id / 目录名 / `preset.id` 三者一致，分类目录下有未注册的包会直接报错

大约 100-200 行代码即可完成一个新预设。

要开一个新分类：在根注册器 `scripts/presets/index.js` 的 `CATEGORY_ORDER` 里加一个 id，建同名目录并放一个分类桶 `index.js`（哪怕 `presets: []`），画廊会自动把它排进标签页。
